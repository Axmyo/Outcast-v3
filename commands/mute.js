const { MessageEmbed } = require("discord.js");


module.exports = {
  name: "mute",
  description: "Mute people from sending mesages on your server.",
  cooldown: 5,
  aliases: ["tape"],
  execute(message, args) {
    
  }
}
